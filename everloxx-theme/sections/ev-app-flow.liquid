<style>
.ev-flow {
  padding: 100px 20px;
  background: #1A1614;
  color: #ffffff;
  margin-left: calc(-50vw + 50%);
  margin-right: calc(-50vw + 50%);
  width: 100vw;
  overflow: hidden;
}
.ev-flow-inner {
  max-width: 1200px;
  margin: 0 auto;
}
.ev-flow h2 {
  font-family: 'Times New Roman', Georgia, serif;
  font-weight: 300;
  font-size: clamp(2rem, 4vw, 3.5rem);color:#ffffff;
  text-align: center;
  margin: 0 0 16px;
  letter-spacing: -0.01em;
}
.ev-flow-sub {
  text-align: center;
  color: rgba(255,255,255,0.6);
  margin-bottom: 64px;
  font-size: 1.1rem;
}
.ev-flow-steps {
  display: flex;
  gap: 0;
  align-items: stretch;
  position: relative;
}
.ev-flow-step {
  flex: 1;
  text-align: center;
  padding: 32px 16px;
  opacity: 0;
  transform: translateY(40px);
  transition: all 0.6s cubic-bezier(0.16, 1, 0.3, 1);
  position: relative;
}
.ev-flow-step.visible {
  opacity: 1;
  transform: translateY(0);
}
.ev-flow-step:not(:last-child)::after {
  content: '\2192';
  position: absolute;
  right: -8px;
  top: 50%;
  transform: translateY(-50%);
  color: #efd2a7;
  font-size: 1.5rem;
  opacity: 0.5;
}
.ev-flow-icon {
  width: 64px;
  height: 64px;
  margin: 0 auto 20px;
  background: rgba(239,210,167,0.18);
  border-radius: 16px;
  display: flex;
  align-items: center;
  justify-content: center;
}
.ev-flow-icon svg {
  width: 28px;
  height: 28px;
  stroke: #efd2a7;
  fill: none;
  stroke-width: 1.5;
  stroke-linecap: round;
  stroke-linejoin: round;
}
.ev-flow-title {
  font-weight: 600;
  font-size: 1.1rem;
  margin-bottom: 8px;
  color: #ffffff;
}
.ev-flow-desc {
  font-size: 1.1rem;
  color: rgba(255,255,255,0.6);
  line-height: 1.5;
}
.ev-flow-badge {
  display: inline-block;
  margin-top: 40px;
  padding: 8px 20px;
  border: 1px solid rgba(239,210,167,0.5);background:rgba(239,210,167,0.1);
  border-radius: 20px;
  color: #efd2a7;
  font-size: 1.1rem;
  letter-spacing: 0.08em;
}
@media (max-width: 749px) {
  .ev-flow { padding: 48px 16px !important; }
  .ev-flow-inner { max-width: 100% !important; }
  .ev-flow-sub { margin-bottom: 32px; }
  .ev-flow-steps { flex-direction: column; gap: 8px; }
  .ev-flow-step { padding: 20px 12px; }
  .ev-flow-step:not(:last-child)::after { content: '\2193'; right: auto; top: auto; bottom: -16px; left: 50%; transform: translateX(-50%); }
}
</style>

<section class="ev-flow">
  <div class="ev-flow-inner">
    <h2>Von der App in dein Zuhause</h2>
    <p class="ev-flow-sub">In 5 Schritten zur perfekten Wandfarbe</p>
    <div class="ev-flow-steps" id="ev-flow-steps">
      <div class="ev-flow-step">
        <div class="ev-flow-icon"><svg viewBox="0 0 24 24"><rect x="5" y="2" width="14" height="20" rx="2" ry="2"/><line x1="12" y1="18" x2="12.01" y2="18"/></svg></div>
        <div class="ev-flow-title">App öffnen</div>
        <div class="ev-flow-desc">Kostenlos für iOS & Android</div>
      </div>
      <div class="ev-flow-step">
        <div class="ev-flow-icon"><svg viewBox="0 0 24 24"><path d="M12 22a1 1 0 0 1 0-20 10 9 0 0 1 10 9 5 5 0 0 1-5 5h-2.25a1.75 1.75 0 0 0-1.4 2.8l.3.4a1.75 1.75 0 0 1-1.4 2.8z"/></svg></div>
        <div class="ev-flow-title">Farbe wählen</div>
        <div class="ev-flow-desc"><span data-ev-count="{n} Premium-Farbtöne">132 Premium-Farbtöne</span> durchstöbern</div>
      </div>
      <div class="ev-flow-step">
        <div class="ev-flow-icon"><svg viewBox="0 0 24 24"><path d="M2 3h6a4 4 0 0 1 4 4v14a3 3 0 0 0-3-3H2z"/><path d="M22 3h-6a4 4 0 0 0-4 4v14a3 3 0 0 1 3-3h7z"/></svg></div>
        <div class="ev-flow-title">AR Vorschau</div>
        <div class="ev-flow-desc">Deine Wand live in der neuen Farbe sehen</div>
      </div>
      <div class="ev-flow-step">
        <div class="ev-flow-icon"><svg viewBox="0 0 24 24"><circle cx="9" cy="21" r="1"/><circle cx="20" cy="21" r="1"/><path d="M1 1h4l2.68 13.39a2 2 0 0 0 2 1.61h9.72a2 2 0 0 0 2-1.61L23 6H6"/></svg></div>
        <div class="ev-flow-title">Bestellen</div>
        <div class="ev-flow-desc">Direkt aus der App — versandkostenfrei</div>
      </div>
      <div class="ev-flow-step">
        <div class="ev-flow-icon"><svg viewBox="0 0 24 24"><path d="m3 9 9-7 9 7v11a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z"/><polyline points="9 22 9 12 15 12 15 22"/></svg></div>
        <div class="ev-flow-title">Genießen</div>
        <div class="ev-flow-desc">Rolle oder Pinsel — sofort wohnbereit</div>
      </div>
    </div>
    <div style="text-align:center;">
      <span class="ev-flow-badge">Kostenlose App · Keine Verpflichtung</span>
    </div>
  </div>
</section>

<script>
(function(){
  var steps = document.querySelectorAll('.ev-flow-step');
  if(!steps.length) return;
  var observer = new IntersectionObserver(function(entries){
    entries.forEach(function(e){
      if(e.isIntersecting){
        var idx = Array.from(steps).indexOf(e.target);
        setTimeout(function(){ e.target.classList.add('visible'); }, idx * 150);
      }
    });
  }, {threshold: 0.2});
  steps.forEach(function(s){ observer.observe(s); });
})();
</script>

{% schema %}
{
  "name": "EVERLOXX App Flow",
  "tag": "section",
  "class": "ev-section",
  "settings": []
}
{% endschema %}
